<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/style.css">
    <title>Whats GROWING ON - Index</title>
</head>

<body>
    <header>
        <%- include('../nav.ejs') %>
    </header>
    <div class="container">
        <% plants.forEach(function(plant) { %>
        <div class="card">
            <div class="title">
                <h3>Plant Name: <%= plant.name %></h3>
            </div>
            <div class="circle">🌞</div>
            <div class="content">
                <h6>Date Planted: <%= plant.date %></h6>
                <h6>Harvest Date: <%= plant.harvest %></h6>
                <h6>Partial or Full Sun: <%= plant.sun %></h6>
                <h6>Notes: <%= plant.notes %></h6>
                <a href="/plants/<%= plant._id %>/edit">Edit Plant</a>
                <a href="/plants/<%= plant._id %>?_method=DELETE" class="delete-form" method="POST">Delete Plant</a>
                </div>
            <% }) %>
        </div>
    </div>

</body>

</html>